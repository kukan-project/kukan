---
title: REST API
description: KUKAN ネイティブの REST API エンドポイント一覧
sidebar:
  order: 2
---

ベースパス: `/api/v1`

認証列の記号:

- **公開** — 認証不要
- **認証** — ログインまたは API トークンが必要
- **組織** — 組織の member / editor / admin のいずれか
- **sysadmin** — システム管理者のみ

:::note[PUT セマンティクス]
PUT エンドポイントは全フィールド置換です。リクエストに含めないフィールドは `null`（またはデフォルト値）にクリアされます。
既存値を保持するには、現在のデータを GET で取得し、変更を加えた全体を送信してください。
例外として、**下書き（draft）状態のデータセットへの PUT は部分更新**です（後述の「下書き（draft）」参照）。
:::

## データセット (packages)

| Method | Path | 説明 | 認証 |
|--------|------|------|------|
| GET | `/packages` | データセット一覧（検索・絞り込み対応） | 公開 |
| POST | `/packages` | データセット作成（`resources` 配列でリソース同時作成可） | 組織 editor |
| POST | `/packages/drafts` | 下書き作成（全フィールド任意） | 認証 |
| GET | `/packages/{nameOrId}` | データセット詳細 | 公開 |
| PUT | `/packages/{nameOrId}` | データセット更新（active は全置換 / draft は部分更新、`resources` は対象外） | 組織 editor |
| DELETE | `/packages/{nameOrId}` | データセット削除（active は論理削除 / draft は即時完全削除） | 組織 editor |
| POST | `/packages/{nameOrId}/publish` | 下書きを公開（`draft` → `active`） | 組織 editor |
| POST | `/packages/{nameOrId}/restore` | 論理削除済みを復元 | sysadmin |
| POST | `/packages/{nameOrId}/purge` | 論理削除済みを完全削除 | 組織 admin |
| GET | `/packages/{id}/resources` | リソース一覧 | 公開 |
| POST | `/packages/{id}/resources` | リソース追加 | 組織 editor |
| PUT | `/packages/{id}/resources/reorder` | リソース順序変更 | 組織 editor |

### 主なクエリパラメータ（GET /packages）

| パラメータ | 説明 |
|----------|------|
| `q` | 検索クエリ（キーワードまたは自然文。ハイブリッド検索） |
| `semantic` | `false` でキーワード一致のみ（デフォルト: `true`） |
| `offset` / `limit` | ページネーション（最大 100） |
| `organization[]` | 組織で絞り込み |
| `groups[]` | カテゴリーで絞り込み |
| `tags[]` | タグで絞り込み |
| `res_format[]` | リソース形式で絞り込み（例: `CSV`） |
| `license_id[]` | ライセンスで絞り込み |
| `state` | `active` / `deleted` / `draft`（`draft` は要認証・後述） |
| `my_org` | 自分が `editor` 以上の組織に限定 |
| `private` | 非公開データを含む |
| `sort_by` | `updated` / `created` / `name` |
| `sort_order` | `asc` / `desc` |
| `include_facets` | ファセット情報を含める |

### 例: データセット検索

```bash
curl "https://your-kukan-site/api/v1/packages?q=人口&organization[]=stat-bureau&limit=10"
```

### 下書き（draft）

データセットは下書きとして作成し、公開条件を満たしてから公開できます。
下書きは公開されるまで検索・一覧（CKAN 互換 API 含む）に一切現れません。

**作成** — `POST /packages/drafts`（要認証）:

全フィールド任意です。`name` を省略すると `untitled-xxxxxxxx` 形式のプレースホルダーが
自動生成されます（プレースホルダーのままでは公開できません）。作成時に `ownerOrg` を
指定する場合は、その組織の editor 権限が必要です。

```bash
curl -X POST https://your-kukan-site/api/v1/packages/drafts \
  -H 'Authorization: Bearer <token>' \
  -H 'Content-Type: application/json' \
  -d '{"title": "作業中のデータセット"}'
```

**公開** — `POST /packages/{nameOrId}/publish`:

`draft` → `active` の一方向遷移です（公開後に下書きへ戻す操作はありません）。
以下の 3 つが未設定の場合は `400` を返します。

- `name` が明示的に設定済み（自動生成プレースホルダーのままは不可）
- `ownerOrg` が設定済み
- `licenseId` が設定済み

権限はデータセット更新権限と同一で、`ownerOrg` の組織の editor 権限が公開時点で
再検証されます。公開済み（`active`）への再実行は許可され、公開時の検索インデックス
同期を再実行します（同期が失敗した場合のリトライ入口として冪等）。

**一覧** — `GET /packages?state=draft`（要認証）:

自分が作成した下書きと、所属組織（editor 以上）の下書きのみが返ります。
対応フィルタは `q` / `name` / `organization[]` / `private` / `sort_by` / `sort_order` /
`offset` / `limit`。`tags[]` / `groups[]` / `res_format[]` / `license_id[]` /
`creator_user_id` / `my_org` / `include_facets` を指定すると `400` になります。
削除が途中で失敗した行（削除未完了）も一覧に含まれます。

**取得・更新・削除**:

- 詳細取得は `GET /packages/{id}?state=draft`（要認証・下書きの閲覧権限が必要）
- draft への `PUT` は**部分更新**です。リクエストに含めたキーのみ適用され、明示的な
  `null` はそのフィールドをクリアします。`name: null` はプレースホルダーを再生成して
  「名前未設定」に戻し、`ownerOrg: null` は組織をクリアします（いずれも公開が再び
  ブロックされます）。`active` への `PUT` は従来どおり全置換です
- draft への `DELETE` はゴミ箱を経由せず**即時完全削除**します（復元不可）。削除が
  途中で失敗した場合は、同じ `DELETE` の再実行が完全削除を完了します

## リソース (resources)

| Method | Path | 説明 | 認証 |
|--------|------|------|------|
| GET | `/resources/count` | アクティブなリソース数 | 公開 |
| GET | `/resources/formats` | 使われているフォーマット一覧 | 公開 |
| GET | `/resources/{id}` | リソース詳細 | 公開 |
| GET | `/resources/{id}/text` | テキストコンテンツ取得（プレビューサイズ制限） | 公開 |
| GET | `/resources/{id}/download` | ファイルダウンロード（外部 URL は 302 リダイレクト） | 公開 |
| GET | `/resources/{id}/preview` | サーバー経由プレビュー（Range 対応） | 公開 |
| GET | `/resources/{id}/pipeline-status` | パイプライン処理状態 | 公開 |
| GET | `/resources/{id}/schema` | 列スキーマ（列名・型・行数・数値統計）。CSV/TSV のみ | 公開 |
| POST | `/resources/{id}/query` | サーバーサイド DuckDB クエリ（SELECT 限定）。CSV/TSV のみ | 公開 |
| POST | `/resources/{id}/upload-url` | プリサインドアップロード URL 発行 | 組織 editor |
| POST | `/resources/{id}/upload` | サーバー経由アップロード | 組織 editor |
| POST | `/resources/{id}/upload-complete` | アップロード完了通知・パイプライン投入 | 組織 editor |
| POST | `/resources/{id}/run-pipeline` | パイプライン手動実行 | 組織 editor |
| PUT | `/resources/{id}` | リソース更新（全フィールド置換、extras はシステム管理） | 組織 editor |
| DELETE | `/resources/{id}` | リソース削除 | 組織 editor |

### スキーマとデータクエリ

CSV/TSV リソース（パイプラインでプレビュー Parquet を生成済み・≤50MB）は、**列スキーマの取得**と
**読み取り専用 SQL クエリ**ができます。AI エージェント向けには MCP の `get_resource_schema` /
`query_resource` ツールも同等の機能を提供します。

**スキーマ取得** — `GET /resources/{id}/schema`:

```json
{
  "id": "…",
  "queryable": true,
  "schema": {
    "rowCount": 1688,
    "columns": [
      { "name": "市区町村（発生地）", "type": "string", "nullable": true, "nullCount": 3 },
      {
        "name": "重量制限",
        "type": "integer",
        "nullable": false,
        "nullCount": 0,
        "stats": { "min": "600", "max": "1350" }
      }
    ]
  }
}
```

クエリ不可のリソース（非対応フォーマット・未処理）は `queryable: false`, `schema: null` を返します。
`type` は `integer` / `float` / `boolean` / `string`。`stats` は数値列のみで、整数の min/max は十進文字列です。

**データクエリ** — `POST /resources/{id}/query`:

```bash
curl -X POST https://your-kukan-site/api/v1/resources/{id}/query \
  -H 'Content-Type: application/json' \
  -d '{"sql": "SELECT \"市区町村（発生地）\" AS city, count(*) AS n FROM data GROUP BY 1 ORDER BY n DESC LIMIT 3"}'
```

```json
{
  "id": "…",
  "columns": ["city", "n"],
  "rows": [
    { "city": "足立区", "n": "117" },
    { "city": "江戸川区", "n": "110" },
    { "city": "大田区", "n": "86" }
  ],
  "rowCount": 3,
  "truncated": false,
  "elapsedMs": 31
}
```

クエリのルール:

- データは `data` という名前のテーブルとして参照します。列名に空白や非 ASCII を含む場合は二重引用符で囲みます。
- 単一の `SELECT` / `WITH` 文のみ。`INSERT` / `UPDATE` / `DELETE` / `CREATE` / `DROP` / `ATTACH` / `COPY` / `PRAGMA` 等の書き込み・DDL は拒否されます。
- 使い捨ての in-memory DuckDB 上で実行され、ファイル・URL・拡張ロード等の外部アクセスは全面的に禁止されます。

制限とエラー:

| 項目 | 値 |
|------|------|
| 最大行数 | 10,000（超過時は `truncated: true`） |
| 最大結果サイズ | 5 MB |
| 実行タイムアウト | 15 秒（`408`） |
| 同時実行 | 1（超過時 `429`） |

| エラー | 状況 |
|------|------|
| `400` | SQL 不正・非 SELECT・長さ超過・クエリ不可リソース |
| `404` | リソース不存在 / 非公開で権限なし |
| `408` | 実行タイムアウト |
| `429` | 同時クエリ上限 |

## 組織 (organizations)

| Method | Path | 説明 | 認証 |
|--------|------|------|------|
| GET | `/organizations` | 組織一覧 | 公開 |
| POST | `/organizations` | 組織作成 | sysadmin |
| GET | `/organizations/{nameOrId}` | 組織詳細 | 公開 |
| PUT | `/organizations/{nameOrId}` | 組織更新（全フィールド置換） | 組織 admin |
| DELETE | `/organizations/{nameOrId}` | 組織論理削除 | 組織 admin |
| POST | `/organizations/{nameOrId}/restore` | 論理削除済みを復元 | sysadmin |
| POST | `/organizations/{nameOrId}/purge` | 論理削除済みを完全削除（SQS 経由、非同期） | sysadmin |
| GET | `/organizations/{nameOrId}/members` | メンバー一覧 | 組織 member |
| POST | `/organizations/{nameOrId}/members` | メンバー追加・ロール変更 | 組織 admin |
| DELETE | `/organizations/{nameOrId}/members/{userId}` | メンバー削除 | 組織 admin |

メンバーのロールは `admin` / `editor` / `member` のいずれか。

## カテゴリー (groups)

| Method | Path | 説明 | 認証 |
|--------|------|------|------|
| GET | `/groups` | カテゴリー一覧 | 公開 |
| POST | `/groups` | カテゴリー作成 | sysadmin |
| GET | `/groups/{nameOrId}` | カテゴリー詳細 | 公開 |
| PUT | `/groups/{nameOrId}` | カテゴリー更新（全フィールド置換） | カテゴリー admin |
| DELETE | `/groups/{nameOrId}` | カテゴリー削除 | カテゴリー admin |
| POST | `/groups/{nameOrId}/restore` | 論理削除済みを復元 | sysadmin |
| POST | `/groups/{nameOrId}/purge` | 論理削除済みを完全削除 | sysadmin |
| GET | `/groups/{nameOrId}/members` | メンバー一覧 | カテゴリー member |
| POST | `/groups/{nameOrId}/members` | メンバー追加・ロール変更 | カテゴリー admin |
| DELETE | `/groups/{nameOrId}/members/{userId}` | メンバー削除 | カテゴリー admin |

## タグ (tags)

| Method | Path | 説明 | 認証 |
|--------|------|------|------|
| GET | `/tags` | タグ一覧 | 公開 |
| GET | `/tags/{id}` | タグ詳細 | 公開 |

## ユーザー (users)

| Method | Path | 説明 | 認証 |
|--------|------|------|------|
| GET | `/users/me` | 自分の情報 | 認証 |
| GET | `/users/me/organizations` | 所属組織一覧 | 認証 |
| GET | `/users` | ユーザー検索（メンバー管理用） | 認証 |

## API トークン (api-tokens)

| Method | Path | 説明 | 認証 |
|--------|------|------|------|
| POST | `/api-tokens` | トークン発行 | 認証 |
| GET | `/api-tokens` | 自分のトークン一覧 | 認証 |
| DELETE | `/api-tokens/{id}` | トークン失効 | 認証 |

## 管理 (admin)

すべて sysadmin 限定。

| Method | Path | 説明 |
|--------|------|------|
| GET | `/admin/search/stats` | 検索インデックス統計（件数・サイズ・DB カウント） |
| GET | `/admin/search/doc/{index}/{id}` | インデックスドキュメント取得（index: `packages` / `resources` / `contents`） |
| GET | `/admin/search/browse/{index}` | インデックスドキュメント一覧（ページング・検索対応） |
| POST | `/admin/reindex-metadata` | 検索インデックス再構築（SQS 経由、非同期） |
| POST | `/admin/jobs/enqueue-all` | 全リソースのパイプライン一括予約 |
| GET | `/admin/jobs/stats` | パイプラインジョブ統計 |
| GET | `/admin/jobs` | パイプラインジョブ一覧 |
| DELETE | `/admin/data` | 全データ削除（ユーザー除く） |
| GET | `/admin/health/stats` | ヘルスチェック統計 |
| GET | `/admin/health` | ヘルスチェック結果一覧 |
| GET | `/admin/users/stats` | ユーザー統計 |
| GET | `/admin/users` | ユーザー一覧 |
| POST | `/admin/users` | ユーザー作成 |
| PATCH | `/admin/users/{userId}` | ユーザー更新 |
| DELETE | `/admin/users/{userId}` | ユーザー論理削除（セッション・トークン即時失効） |
| POST | `/admin/users/{userId}/restore` | ユーザー復元 |
| POST | `/admin/users/{userId}/purge` | ユーザー完全削除 |

## その他

| Method | Path | 説明 | 認証 |
|--------|------|------|------|
| GET | `/api/health` | ヘルスチェック | 公開 |
| GET | `/api/v1/site/settings` | サイト設定（登録可否等） | 公開 |
