---
title: システム管理者ガイド
description: KUKAN のデプロイ・運用・システム管理
sidebar:
  order: 0
---

## デプロイ方法

KUKAN は2つのデプロイ方式に対応しています。手軽な Docker Compose（オンプレミス・閉域網向け）、
本番のクラウド向け AWS の順で説明します。

### Docker Compose（オンプレミス）

Docker Compose を使ったオンプレミス・閉域網デプロイに対応しています。
本番運用では最新の[リリース](https://github.com/kukan-project/kukan/releases)（`vX.Y.Z` タグ）の利用を推奨します。

```bash
git checkout vX.Y.Z   # 最新リリース
cp .env.prod.example .env.prod
# .env.prod を編集
docker compose --env-file .env --env-file .env.prod --profile prod up -d --build
```

#### アップデート

アップグレード前に[リリースノート](https://github.com/kukan-project/kukan/releases)で破壊的変更と移行手順を確認してください。

:::caution[リリースノートが「先に止める」よう指示する版]
`up -d --build` はサービスを個別に作り直します。新しい Worker がマイグレーションを適用した時点で、旧 web コンテナがまだ認証を処理していると、サインインが失敗します。該当する版では、先にアプリケーションを止めてから起動してください。

```bash
docker compose --env-file .env --env-file .env.prod --profile prod down
```

適用後のデータベースでは、それより前のイメージが動かないことがあります（v0.22.0 では 0.17.0〜0.21.x を含む旧イメージすべてで認証が失敗します）。イメージだけを戻しても復旧しないので、問題があれば新しい版へ進めて直してください。
:::

```bash
git fetch --tags
git checkout vX.Y.Z   # 新しいリリース
docker compose --env-file .env --env-file .env.prod --profile prod up -d --build
```

データベースのマイグレーションは Worker の起動時に自動適用されます。適用後のデータベースに旧バージョンのイメージを戻せるとは限りません。

:::note[バージョン管理を導入したリリースへ上げるとき]
アップグレード後、サイト管理者のダッシュボードに「**バージョンの補完**」が表示されます。
これを実行するまで、それ以前からあるリソースは差し替えても旧内容が版として残りません
（[データ管理者ガイド](/ja/data-admin-guide/#導入時の一度きりの移行)）。
:::

オンプレミス構成では以下の代替サービスを使用します。

| AWS サービス | オンプレミス代替    |
| ------------ | ------------------- |
| S3           | MinIO               |
| SQS          | ElasticMQ           |
| OpenSearch   | PostgreSQL 全文検索 |

### AWS デプロイ

KUKAN は **CloudFront（WAF + キャッシュ）→ 内部 ALB → ECS Fargate** 構成で AWS にデプロイします。
デプロイは **CDK Pipelines（AWS CodePipeline）で自動化**され、CodeConnections（GitHub App）をソースに、
対象ブランチへの push を起点に各環境（dev / prd 等）を CDK Stage としてデプロイします。

環境は `infra/config/environments.ts` で定義します（`environments.example.ts` を**コピーして作成**）。
環境は**マルチサイト形状（`sites` 宣言）で作成します**。サイトが 1 つでも `sites` 1 件で
始めておくと、後からのサイト追加が非破壊になります（[マルチサイト運用](/ja/system-admin-guide/multi-site/)を参照）。
`sites` を書かず、サイト固有の設定（ドメイン・WAF 等）を env 直下に書くシングルサイト形状は
**過去互換**として引き続き動作しますが、新規構築では使用しません（両形状の混在は不可）。
全パラメータの一覧は[環境設定リファレンス](/ja/system-admin-guide/environment-config/)を参照してください。

主要な AWS リソース:

- **CloudFront** — CDN・WAF・TLS 終端（最前面）
- **ALB（internal）** — ロードバランサー（CloudFront VPC Origin 経由でのみ到達）
- **ECS Fargate** — Web アプリケーション + Pipeline Worker
- **Aurora Serverless v2 / RDS** — PostgreSQL データベース
- **S3** — ファイルストレージ
- **SQS** — パイプラインキュー
- **OpenSearch** — 全文検索エンジン
- **AWS Backup** — 隔離バックアップ（large スケール等で有効）

#### デプロイ通知（Slack）

環境に `deployNotification: true` を置くと、その環境のデプロイが終わるたびに成否が Slack に流れます。
省略した環境には通知用のリソースが 1 つも作られません。

webhook URL は [Slack のアプリ管理画面](https://api.slack.com/apps) で **Create New App**（From scratch）→ **Incoming Webhooks**
を有効化 → **Add New Webhook to Workspace** で投稿先チャンネルを選ぶと発行されます。

貼り先の箱はスタックが作ります。`deployNotification: true` でデプロイするとシークレットの ARN が
パイプラインスタックの出力に出るので、そこへ一度貼ります:

```bash
aws secretsmanager put-secret-value --secret-id <出力の ARN> --secret-string 'https://hooks.slack.com/services/...'
```

貼るまでの間、通知は何もしません（デプロイは失敗しません）。通知には成否・デプロイしたコミット・
更新したサイト（`domainName` があればリンク）・リリースノートへのリンク（リリース時のみ）・
パイプライン実行へのリンクが入ります。

:::caution
一度有効にした後に `deployNotification` を外すと、**貼った webhook URL ごと削除されます**。
もう一度有効にすると別のシークレットが作られるため、URL は貼り直しになります。
:::

#### バックアップとデータ保護

データ保護の設定は `scale` プリセットに統合されています:

| 設定                                    | small | medium | large                 |
| --------------------------------------- | :---: | :----: | :-------------------- |
| S3 バージョニング（誤削除・上書き保護） |   —   |   ✓    | ✓                     |
| DB 自動バックアップ保持（任意時点復元） |  7日  |  14日  | 35日                  |
| AWS Backup（隔離保管・長期世代）        |   —   |   —    | 日次35日 + 月次12ヶ月 |

アップロードファイルは S3 に保存され、リージョン内の複数アベイラビリティゾーンへ自動複製されます（設計耐久性 99.999999999%）。バージョニング有効時は誤削除・上書きからの復旧も可能です。AWS Backup は S3 と DB のバックアップを独立した Backup Vault に隔離保管し、リソース本体ごと失われる事故や 35 日を超える長期世代に備えます。環境ごとの調整は `environments.ts` の `overrides.backup` で行います（`awsBackup` の有効化には `s3Versioning: true` が必要）。

ストレージ内のオブジェクトは、書かれ方ごとにプレフィックスで分かれています。すべて同じバケットに
入るため、バックアップ設定は共通です。

| プレフィックス | そこに書かれるもの                                         |
| -------------- | ---------------------------------------------------------- |
| `resources/`   | リソースの正本。差し替えのたびに新しいオブジェクトが増える |
| `previews/`    | プレビュー Parquet、検索索引用のテキスト                   |
| `lake/`        | 行差分のためのデータファイル（DuckLake）                   |

**プレフィックスはオブジェクトの役割を決めません。** どれが現在の内容でどれが過去の版かは、
データベースのポインタ（`resource.storage_key` / `resource_version.storage_key`）が決めます。
オブジェクトが書かれる時点では、それが版になるかどうかまだ決まっていないためです（取得した内容が
前回と同じなら版は作られません）。

- **現在のファイルも過去の版のファイルも `resources/` にあります。** ストレージ使用量はここで
  増え、差し替えの回数だけ積み上がります
- `resources/` には、まだ完了していないアップロードや、参照されなくなって回収待ちのオブジェクトも
  含まれます

行差分のカタログは同じ PostgreSQL の `ducklake` スキーマに置かれます（追加の環境変数はありません）。

**いずれのプレフィックスもバックアップの対象から外さないでください。** `previews/` と `lake/` は
正本から作り直せる派生データですが、失われた派生をまとめて再生成する運用手順は現時点では用意されて
いません。

#### セットアップ（初回のみ手動。以降は push で自動デプロイ）

1. 環境定義を用意（`cp infra/config/environments.example.ts infra/config/environments.ts` → `environments.ts` を編集）。**各エントリが1環境＝1パイプライン**として作られるため、単一環境なら不要なエントリ（例: `dev`）を削除する
2. AWS コンソールで CodeConnections 接続を作成し、Connection ARN を `environments.ts` のトップレベル `connectionArn`（全 env 共通の export）に設定
   - **CodeBuild** を開く → 左メニュー下部の **Settings → Connections → Create connection**（「Developer Tools」共通設定。CodeBuild 経由が分かりやすい）→ **GitHub** を選択 → 接続名（例 `kukan-github`、AWS 側の識別ラベル）を入力 → **Connect to GitHub** → **AWS Connector for GitHub** アプリをインストール・認可（**Only select repositories** を選び、デプロイ対象のリポジトリのみに絞る。最小権限）→ ステータスが **Available** になったら ARN をコピー
   - GitHub App の認可はブラウザ操作が必須（CLI/CDK で作成しても `PENDING` になり結局ブラウザ認可が必要）
3. `cdk bootstrap`（deploy の前提。各アカウント・リージョン。GlobalStack 用に us-east-1 も含める）
4. 独自ドメイン/WAF を使う場合のみ: us-east-1 の証明書/WAF を一度 standalone で作成（`npx cdk deploy -c env=prd Prd/KukanGlobalStack`）し、ARN を各サイトエントリの `certificateArn`/`webAclArn` に設定
5. `environments.ts` と `cdk.context.json` をコミット（CodeBuild の synth が checkout から読み込む）
6. パイプラインスタックを初回デプロイ（`npx cdk deploy KukanPipeline`）
7. 以降は各 env の `deployBranch` への push で自動デプロイ（パイプラインも自己変異で更新）

`environments.ts` の構造（`connectionArn` はトップレベル、env 個別設定は `environments` の中）:

```ts
// infra/config/environments.ts
export const connectionArn = 'arn:aws:codeconnections:...:connection/...' // 手順2の ARN（全 env 共通）

export const environments = {
  // account は必須（誤デプロイ防止）。同一アカウントなら全 env に同じ ID を指定
  dev: {
    account: '000000000000',
    scale: 'small',
    githubRepo: 'owner/repo',
    deployBranch: 'develop',
    sites: [{ name: 'main', enableWaf: false }],
  },
  prd: {
    account: '000000000000',
    scale: 'large',
    githubRepo: 'owner/repo',
    deployBranch: 'main',
    sites: [{ name: 'main', enableWaf: false }],
  },
}
```

:::caution
CodeConnections の GitHub App 承認は、コンソールでの一度きりの手動操作です（IaC 化できません）。また CDK Pipelines は cross-region 参照と非互換のため、独自ドメイン/WAF（us-east-1）は手順4で ARN を供給するか `enableWaf: false` にしてください。prd には承認ゲート（手動承認）の設定を推奨します。
:::

:::note
パイプラインの synth は CodeBuild 上で実行されます。context lookup（AZ・CloudFront プレフィックスリスト）を決定的にするため、`infra/cdk.context.json` をコミットしてください（未コミットでも synth ロールの lookup ロール権限で解決されますが非決定的）。
:::

詳細・設計判断はリポジトリの `docs/specs/jp/phase4-deploy.md` および ADR-030（`docs/adr/jp/030-cdk-pipelines-deploy.md`）/ ADR-031（`docs/adr/jp/031-multi-environment-deploy.md`）を参照してください。

## 初期セットアップ

ユーザーが1人も存在しない間は自己登録が強制的に有効になり、**最初に登録した
ユーザーが自動的に sysadmin になります**。デプロイ後、ブラウザでサインアップ
ページを開いて初回ユーザーを登録してください。

:::caution
最初の登録が完了するまでは、URL に到達できた人が誰でも sysadmin になれます。
インターネットに公開するデプロイでは、デプロイ後すみやかに初回ユーザーを
登録してください。
:::

ブラウザを使わずに初期化する場合や、sysadmin が全員いなくなった場合の回復には
CLI を使います。

```bash
pnpm db:create-user --email admin@example.com --name admin --password <password> --role sysadmin
```

| オプション   | 説明                                                           | 必須 |
| ------------ | -------------------------------------------------------------- | ---- |
| `--email`    | メールアドレス                                                 | Yes  |
| `--name`     | ユーザー名（小文字英数字、ハイフン、アンダースコア、ピリオド） | Yes  |
| `--password` | パスワード（8文字以上）                                        | Yes  |
| `--role`     | `user` または `sysadmin`（デフォルト: `user`）                 | No   |

## ユーザー管理

システム管理者（sysadmin）は管理画面からユーザーを管理できます。

### ユーザー一覧・編集

- ユーザーの表示名・役割（user / sysadmin）の変更
- 自分自身の役割降格は不可（ロックアウト防止）

### ユーザーの削除

- ソフトデリート方式（state を deleted に変更）
- 削除時に全セッション・API トークンを即時無効化
- 監査ログ・アクティビティ履歴は保持

### 自己登録制御

新規ユーザーの自己登録は管理画面（サイト管理 → ユーザー自己登録）で
切り替えます（デフォルト: 無効）。無効の間はシステム管理者が管理画面から
ユーザーを作成します。ユーザーが1人も存在しない間は、初期セットアップの
ため設定に関係なく自己登録が有効になります。

## サイト管理設定（ランタイム設定）

一部の設定は**再デプロイなしに管理画面から変更**できます（サイト管理 → `/dashboard/admin/site`、システム管理者のみ）。変更は DB に保存されて監査ログに残り、反映まで最大 30 秒ほどかかります。

| 設定                               | 内容                                                                                                                                                                                 |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 検索例チップ                       | 検索ボックス下に表示するクエリ例                                                                                                                                                     |
| セマンティック検索・類似度しきい値 | 意味の近い結果を検索に含めるかどうかと、含める「意味の近さ」の下限。緩めるとヒットが増える代わりにノイズも増え、厳しくすると精度重視になります（要: 埋め込みモデルが利用可能な構成） |
| AI メタデータ提案                  | 機能の有効化（キルスイッチ）、生成モデル ID（空欄はプロバイダ既定）、接続テスト                                                                                                      |
| AI による説明（抄録）の生成言語    | 抄録を書く言語（日本語 / 英語）。既定は日本語。言語を変えても既存の抄録は書き直されません（下記「[AI による説明（抄録）](#ai-による説明抄録)」の一括生成が要ります）                 |
| ユーザー自己登録                   | 上記「[自己登録制御](#自己登録制御)」を参照                                                                                                                                          |

### AI・埋め込みモデルの選択

セマンティック検索（埋め込み）と AI メタデータ提案は、`AI_TYPE` で選んだプロバイダを使います。

- **AWS**: Bedrock（`AI_TYPE=bedrock`）
- **オンプレミス / 開発**: Ollama（`AI_TYPE=ollama`）または OpenAI 互換（`AI_TYPE=openai`）
- **無効**: `AI_TYPE=none`（NoOp）— セマンティック検索はキーワード検索にフォールバックし、AI 提案は利用不可になります

生成・埋め込みモデルは管理画面から再デプロイなしで切り替えられます。候補モデルは `AI_COMPLETION_MODELS` 環境変数（カンマ区切り）で指定します。これは**このデプロイで利用を許可するモデルのリスト**で、全プロバイダ共通に管理画面ピッカーの選択肢となり、先頭エントリが既定（「プロバイダ既定」）になります。サーバー側で利用可能でも、リストに無いモデルは候補に現れません。**Bedrock** では CDK が `environments.ts` の `bedrock.completionModels`（IAM で `InvokeModel` を許可したモデル）からこの変数を注入するため、候補を増やす場合はそこに追加して再デプロイしてください。**Ollama** では Docker Compose の `ollama-init` が列挙された全モデルを起動時に pull するため、リストのモデルは自動で利用可能になります。未設定時は組み込み既定モデルのみが候補になります（Ollama: gemma4:e4b / OpenAI 互換: gpt-4o-mini）。Ollama を GPU で動かす手順は[開発環境セットアップ](/ja/getting-started/)を参照してください。

:::caution[小規模ローカルモデルの提案品質]
オンプレミス / CPU-only 環境向けの小規模ローカルモデル（実効 4〜8B クラス、Gemma 4 E4B 等）は、**メタデータ提案の品質としては非力**です。JSON 形式は正しく返しても、説明文が要点を外す・タグが的外れ・日本語の出力が不安定といった内容面の劣化が起きやすくなります。AI 提案はあくまで**下書きの叩き台**と位置づけ、採用前に人が必ず確認・修正する運用にしてください（提案はどのフィールドも初期 OFF で、採用したものだけがメタデータに反映されます）。生成品質を重視する場合は、Bedrock（Amazon Nova Lite 以上、必要なら Claude Haiku 系）や、十分な VRAM を確保した上での上位 Ollama モデル（Gemma 4 の上位サイズ・Qwen3 系）を選んでください。生成モデルは管理画面から再デプロイなしに切り替えられます。

AWS を使わないオンプレミス構成でも、常用の商用 AI に接続して品質を確保する方法があります。`AI_TYPE=openai` のまま `OPENAI_BASE_URL` を OpenAI 互換の商用エンドポイントやゲートウェイ（LiteLLM 等）に向けると、高性能な生成モデルを利用できます（提案機能は構造化出力〈`response_format` の JSON スキーマ〉を用いるため、エンドポイント側がこれに対応している必要があります）。Claude を純正で使う場合は現状 AWS の Bedrock（`AI_TYPE=bedrock`）が唯一の経路で、Anthropic API を API キーで直接叩く専用プロバイダは未提供です。
:::

#### AI 提案が動かないとき（Bedrock）

サイト管理の **接続テスト**を押すと、実際の生成と同じ経路（構造化出力）で 1 回だけ試行し、失敗時はエラーと対処のヒントを表示します。Bedrock 特有の典型的な原因と対処:

| 症状（接続テストのエラー）                       | 原因                                               | 対処                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------ | -------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `not authorized to perform: bedrock:InvokeModel` | タスクロールに当該モデルの IAM 権限がない          | `environments.ts` の `bedrock.completionModels` に追加して再デプロイ                                                                                                                                                                                                                                     |
| `use case details have not been submitted`       | Anthropic の利用ユースケースフォーム未提出         | Bedrock コンソールで Anthropic の「利用ユースケース詳細」フォームを提出（アカウントに一度・約15分で有効化）                                                                                                                                                                                              |
| `aws-marketplace:Subscribe` 系の AccessDenied    | Marketplace 提供モデルがアカウント未サブスクライブ | 下記「[Marketplace モデルの初回サブスクライブ](#marketplace-モデルの初回サブスクライブ)」の手順で一度 invoke して有効化。組織で SCP により制限されている場合は、**管理アカウント**側で `aws-marketplace:Subscribe` / `ViewSubscriptions` の許可が必要（AdministratorAccess でも SCP は上書きできません） |

これらはいずれも**アプリ外（AWS 側）の設定**で、コンソールでの一度きりの作業です。Claude 等の Anthropic モデルはフォーム提出と Marketplace サブスクライブの両方が必要になる点に注意してください（Amazon Titan / Nova はどちらも不要 — 初回呼び出しで自動有効化されます）。

:::note
かつての Bedrock **Model access（モデルアクセス）** ページは廃止されました。サーバーレス基盤モデルはアカウント内で初回に呼び出した時点で自動的に有効化されるため、手動で「有効化」する操作はもうありません。アクセス制御は IAM ポリシーと SCP に一本化されています。
:::

#### Marketplace モデルの初回サブスクライブ

Cohere Embed v4 のような **AWS Marketplace 提供モデル**は自動有効化の対象外で、アカウント全体で使えるようにするには **AWS Marketplace 権限を持つユーザーが一度だけ呼び出す**必要があります。いずれかの方法で 1 回呼び出せば、以後はデプロイ済みのタスクロールからも利用できます（伝播に数分）。

- **コンソール**: Bedrock → **モデルカタログ（Model catalog）** で対象モデルを開き、**プレイグラウンド（Playground）** で任意の入力を 1 回実行する。有効化済みの一覧は [AWS Marketplace の「Manage subscriptions」](https://console.aws.amazon.com/marketplace/home#/subscriptions)で確認できます。
- **CLI**（同じことを 1 コマンドで）:

  ```bash
  aws bedrock-runtime invoke-model --region ap-northeast-1 \
    --model-id cohere.embed-v4:0 --content-type application/json \
    --body '{"texts":["test"],"input_type":"search_document"}' /dev/stdout
  ```

埋め込みモデルを切り替えた場合は、既存データに反映するため**埋め込みベクトルの再生成**が要ります（下記「[検索インデックスの再処理](#検索インデックスの再処理)」）。

### AI による説明（抄録）

リソースを登録するたびに、**そのファイルの内容を読んだ短い説明**を AI に書かせ、リソースページに表示できます。利用者が原本を開かずに中身を判断できるようにするためのもので、同時に検索の手掛かりにもなります（リソース単位の埋め込みに入ります）。

**有効化は「書かせるモデルを指定する」ことだけです。** 指定すると有効になり、指定しなければ何も生成されず費用もかかりません。AWS では `environments.ts` の `bedrock.summaryModel`、Docker Compose では環境変数 `AI_SUMMARY_MODEL` で指定します。

```ts
// AWS（infra/config/environments.ts）— CDK がここから AI_SUMMARY_MODEL を注入します
bedrock: {
  completionModels: [
    'jp.anthropic.claude-sonnet-4-6',
    'jp.anthropic.claude-haiku-4-5-20251001-v1:0',
    'jp.amazon.nova-2-lite-v1:0',
  ],
  summaryModel: 'jp.anthropic.claude-sonnet-4-6',
}
```

```bash
# Docker Compose（.env）
AI_SUMMARY_MODEL=jp.anthropic.claude-sonnet-4-6
```

:::caution[Bedrock では既定のままでは足りません]
`bedrock.completionModels` を省略したときの既定は **Amazon Nova Lite 1 本**です。`summaryModel` はこの許可リストに含まれるモデルしか受け付けないため（許可リストは IAM で `InvokeModel` を許可した範囲そのものです）、リストに無いモデルを指定すると、デプロイされる前に合成（synth）の時点で拒否されます。

しかも **Nova は抄録には使えません** — 実測では document ブロックは通るものの、強制 JSON が壊れ、スキャン文書では内容を捏造しました（ADR-053 の付録）。既定のモデルで妥協するのではなく、上の例のように `completionModels` に推奨モデルを追加してください。

`completionModels` の先頭が AI メタデータ提案の既定になります。抄録用のモデルは `summaryModel` で別に指定するため、並び順に引きずられません。
:::

:::tip[サイト単位の指定]
マルチサイト環境（ADR-041）では、環境エントリの `bedrock` が共通の設定、`sites[].bedrock` がサイト単位の上書きです。1 つのサイトだけ安いモデル（Sonnet でなく Haiku 4.5 など）にする、あるいは抄録を生成しない、といった指定ができます。

```ts
sites: [
  { name: 'main' }, // 環境の設定をそのまま使う
  { name: 'citya', bedrock: { summaryModel: 'jp.anthropic.claude-haiku-4-5-20251001-v1:0' } },
  { name: 'cityb', bedrock: { summaryModel: undefined } }, // このサイトは抄録なし
  { name: 'cityc', bedrock: false }, // このサイトは AI 自体なし
]
```

Docker Compose でも同じ分け方ができます — サイトごとの env ファイル `docker/multi-site/sites/<site>.env` に `AI_SUMMARY_MODEL` を書きます。
:::

:::caution[有効化すると推論費用が発生します]
抄録はリソース 1 件ごとに生成されるため、**カタログの規模に比例して課金されます**。これが管理画面のトグルではなく環境変数になっているのは、費用を負担する側が決めるべき設定だからです。モデルの選択は請求額で 3 倍の開きがあり、かつ実測では「資料を読んだ説明」と「作文」の差になります — 生成モデルの候補（`AI_COMPLETION_MODELS`）の先頭が何であれ、抄録用のモデルは明示してください。推奨は Claude Sonnet 4.6 相当です。

一括生成を実行する前に、管理画面が**対象件数と推定費用を表示します**。確認してから実行してください。
:::

抄録用のモデルには生成モデルの許可リストに含まれるものを指定します。生成**言語**の方はランタイム設定（サイト管理）で切り替えられます — 言語は無料、モデルは費用、という分け方です。

#### 一括生成（サイト管理 → AI による説明の一括生成）

通常は登録時に自動で生成されるため、操作は要りません。このカードは、**設定を入れる前からあるリソース**や、**モデル・プロンプト版・生成言語が変わって書き直しが要るもの**をまとめて処理するためのものです。外部 URL の再取得は行いません。

| 操作           | 対象                                                                     |
| -------------- | ------------------------------------------------------------------------ |
| 不足分を生成   | 説明がまだ無いリソースだけ                                               |
| すべて作り直す | 上記に加えて、別のモデル・プロンプト版・生成言語で書かれた説明も書き直す |

どちらも押す前に「対象 N 件・推定 入力 X〜Y / 出力 Z トークン（概算 $低〜$高）」が表示されます。入力トークンが幅を持つのは、PDF のトークン数がページ数で決まり、ページ数はここからは見えないためです（下限 1 ページ・上限はページ上限）。

完了したパッケージから順に埋め込みも作り直されます。同じ内容に対して二度実行しても、生成済みかどうかを照合するため費用は増えません。

## 検索インデックスの再処理

検索管理画面（インデックス管理）から、索引や派生データを作り直せます。いずれも SQS 経由の非同期処理で、投入後はバックグラウンドで順次実行されます。

| 操作                     | 作り直すもの                                     | 外部 URL の取得  | 実行するとき                                                                                                                                                                                                                                            |
| ------------------------ | ------------------------------------------------ | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 検索インデックスの再構築 | メタデータ索引                                   | しない           | 検索結果に不整合があるとき。**アップグレードでマッピングやスキーマが変わったとき**（`POST /api/v1/admin/reindex-metadata`）                                                                                                                             |
| コンテンツの再処理       | 保存済みファイルのテキスト抽出・プレビュー・索引 | 未取得のものだけ | 本文検索やプレビューが欠けているとき。**リソース数に比例して時間がかかります**                                                                                                                                                                          |
| 埋め込みベクトルの再生成 | 意味検索用のベクトル                             | しない           | セマンティック検索を後から有効にしたとき、埋め込みモデルを変えたとき（`POST /api/v1/admin/reindex-embeddings`）。内容が変わっていないものはスキップされます。**意味検索の対象外になっているリソースがある間は、ダッシュボードにも案内が出ます**（下記） |
| 検索インデックスの再解析 | 索引そのもの（解析設定ごと）                     | しない           | 日本語の解析設定が更新されたとき。**画面に案内が出たときだけ**（下記）                                                                                                                                                                                  |

通常はどれも不要です。メタデータの索引はデータセットの作成・更新・削除のたびに自動で更新され、本文とプレビューはパイプラインが処理した時点で作られます。

### 検索インデックスの再解析

日本語の形態素解析の設定を更新したリリースへ上げると、検索管理画面に**再解析**の案内が出ます。解析方法は**インデックス作成時に決まる**ため、既存のインデックスは作り直さないと新しい解析が効きません。上の「再構築」は同じ索引に文書を送り直す操作で、解析方法は変わらないため、これだけは再解析でしか反映されません。

保存済みのテキストを読み替えるだけで、**外部 URL の取得もファイルの再処理も行いません**。更新時の一度きりの操作で、完了すると案内は表示されなくなります（`POST /api/v1/admin/search/reanalyse`）。

## 一度きりの移行操作

再処理と違い、**移行のときに一度だけ**実行する操作です。ダッシュボードに残件数が表示され、0 になると項目自体が消えます。

### 埋め込みの再生成

意味検索の対象になっていないリソースがある間、ダッシュボードに案内が表示されます。埋め込みは**ベクトルが無いと検索が失敗せずに黙ってキーワードのみへ縮退する**ため、気づく手立てとして案内を出しています。

出るのは主に 2 つの場合です。**埋め込みの単位をリソースへ移したリリースへ上げた直後**（既存のベクトルは作り直しが必要です）と、**埋め込みモデルを変えたとき**です。通常運用の「編集してから生成されるまでの数十秒」では出ません — 生成待ちのパッケージは数に入れないためです。

案内から実行する処理は、上の表の「埋め込みベクトルの再生成」と同じものです。

### 版の補完

バージョン管理より前からあるリソースには版が付いていません。ダッシュボードの「**バージョンの補完**」を実行すると、各リソースの現在のファイルを v1 として記録し、表形式のものを差分表示に取り込みます。再取得も再インデックスも行いません（`GET /api/v1/admin/version-backfill-status` / `POST /api/v1/admin/backfill-versions`）。

:::caution
**補完しないままファイルを差し替えると、それ以前の内容は版として残らずに失われます。**
バージョン管理を含むリリースへ上げたら、早い段階で実行してください。
:::

層ごとの内訳と、旧方式の巻き戻しの変換については[データ管理者ガイド](/ja/data-admin-guide/#導入時の一度きりの移行)を参照してください。

## ヘルスチェック

外部 URL リソースの死活監視を自動で行います。

- 定期的なスケジューラーによる自動チェック
- 管理画面からヘルスチェック結果の確認
- エラーリソースの特定と対応

## ストレージの回収

差し替えなどで参照されなくなったオブジェクトは台帳に記録され、**毎時のスイープが保持期間の経過後に
削除します**。削除の直前にもう一度「まだ誰か指しているか」を確認するため、通常の運用で操作は要りません。

台帳が導入されたのはバージョン管理と同じリリースです。**それ以前に処理の中断で取り残された
オブジェクトは、どの記録にもポインタにも名前がありません。** 該当するのはこのリリースより前から
運用していた環境だけで、回収するにはバケットを一覧する必要があるため、手動のツールを用意しています。

デプロイ済みのコンテナ内（本番のデータベースとバケットに届く場所）で実行します。

```bash
node apps/worker/dist/reconcile-orphans.js            # 報告のみ
node apps/worker/dist/reconcile-orphans.js --commit   # 台帳へ登録
```

チェックアウトからは次のように実行します。

```bash
pnpm --filter @kukan/worker reconcile-orphans
pnpm --filter @kukan/worker reconcile-orphans -- --commit
```

- **既定は報告のみ**です。対象の鍵が一覧で出るので、`--commit` の前に目を通してください
- **`--commit` も削除はしません。** 台帳に登録するだけで、実際に消すのは毎時のスイープです。
  スイープは削除の直前に参照を確認し直すので、一覧を取った後にポインタが着いたオブジェクトは
  残ります。判断を誤っても失うのは台帳の行であってファイルではありません
- **24 時間以内に書かれたオブジェクトは対象外**です。書き込み途中のオブジェクトは、一覧からは
  取り残しと区別が付かないためで、これらは `tooRecent` として報告されます
- **報告の `nominated` と `tooRecent` が両方 0 になるまで繰り返します。** 一度で終わるとは限りません
- 対象は `resources/` と `previews/` です。`lake/` は DuckLake が自分の配置を知っていて自分で
  回収するため含みません

## 環境変数

主要な環境変数の一覧です。詳細は `.env.example` を参照してください。

| 変数名                 | 説明                                                                              | デフォルト              |
| ---------------------- | --------------------------------------------------------------------------------- | ----------------------- |
| `POSTGRES_HOST`        | PostgreSQL ホスト                                                                 | `localhost`             |
| `POSTGRES_PORT`        | PostgreSQL ポート                                                                 | `5432`                  |
| `POSTGRES_DB`          | データベース名                                                                    | `kukan`                 |
| `POSTGRES_USER`        | データベースユーザー                                                              | `kukan`                 |
| `POSTGRES_PASSWORD`    | データベースパスワード                                                            | `kukan`                 |
| `S3_BUCKET`            | S3 バケット名                                                                     | `kukan-dev`             |
| `S3_ENDPOINT`          | S3 エンドポイント（MinIO: `http://localhost:9000`、AWS: 省略）                    | -                       |
| `SEARCH_TYPE`          | 検索エンジン（`opensearch` / `postgres`）                                         | `opensearch`            |
| `OPENSEARCH_URL`       | OpenSearch エンドポイント                                                         | `http://localhost:9200` |
| `SQS_QUEUE_URL`        | SQS キュー URL                                                                    | -                       |
| `SQS_ENDPOINT`         | SQS エンドポイント（ElasticMQ: `http://localhost:9324`、AWS: 省略）               | -                       |
| `BETTER_AUTH_SECRET`   | 認証セッション秘密鍵（32 文字以上）                                               | -                       |
| `BETTER_AUTH_URL`      | 公開 URL                                                                          | `http://localhost:3000` |
| `PASSWORD_MIN_SCORE`   | パスワード強度の下限（zxcvbn スコア 0〜4）— 下記                                  | `3`                     |
| `AI_TYPE`              | AI プロバイダ（`none` / `bedrock` / `openai` / `ollama`）                         | `none`                  |
| `AI_COMPLETION_MODELS` | 生成モデルの許可リスト（カンマ区切り）。管理画面ピッカーの選択肢、先頭が既定      | -                       |
| `AI_SUMMARY_MODEL`     | AI による説明を書くモデル。**未設定で機能オフ**（費用も発生しない）— 上記参照     | -                       |
| `LOG_LEVEL`            | ログレベル（`trace`〜`fatal`）                                                    | `info`                  |
| `TIME_ZONE`            | 日時を事前描画するタイムゾーン（IANA 名）。ブラウザ側で閲覧者のゾーンに置き換わる | `Asia/Tokyo`            |

### パスワード強度ポリシー

パスワードを設定するすべての経路 — サインアップ、利用者自身による変更、管理画面での
ユーザー作成、`pnpm db:create-user` — に同じ条件が適用されます。

- **15 文字以上**（変更不可）
- **zxcvbn スコアが `PASSWORD_MIN_SCORE` 以上**（既定値 `3` =「安全に推測困難」）

`PASSWORD_MIN_SCORE` を下げると、よく使われる単語の並びやキーボード上の並びが通るように
なります。`0` では推測困難性の判定そのものが無効になります。

:::caution
`PASSWORD_MIN_SCORE` は開発環境向けの設定です。**本番環境では設定しないでください。**
ローカルで使い捨てのパスワードを使い続けるためのもので、値を下げることは
そのまま認証の強度を下げることを意味します。文字数の下限はこの設定に関わらず適用されます。
:::
