/**
 * KUKAN API - Library Entry Point
 * Exports createApp and types for embedding into Next.js or standalone use
 */
export { createApp } from './app'
export { closePool } from '@kukan/db'
export type { AppContext } from './context'
