# ADR-034: メタデータのベクトル検索（セマンティック検索・AI 向けデータセット発見）

## ステータス

**承認（Accepted）** — 全決定を実装済み（Phase 5a）。埋め込みモデルの選定と類似度
しきい値はゴールデンセット評価で確定した（後述「評価結果」、2026-07-07）。

## コンテキスト

現行の検索は BM25（OpenSearch）/ ILIKE + 全文検索（PostgreSQL）によるキーワードマッチであり
（ADR-009）、次の限界がある:

- **語彙の不一致**: 「ごみ収集」で検索しても「廃棄物」と書かれたデータセットがヒットしない。
- **自然文クエリに弱い**: 「子育て支援の施設はどこ?」のような文での検索に対応できない。
- **AI エージェントからの発見**: ADR-032 の MCP 基盤により AI エージェントがカタログを探索する
  ユースケースが確立した。エージェントは自然文でデータセットを探すため、キーワード一致のみでは
  「探索 → スキーマ → クエリ」ループの入口（発見）がボトルネックになる。

そこで、メタデータ（package の title / notes / tags に所属リソースの name / description を
連結したテキスト）を埋め込みベクトル化し、BM25 と融合したハイブリッド検索を導入する。
リソースメタデータを含めるのは、BM25 側が既に kukan-resources インデックスを検索対象に
含めており（ADR-025）、ベクトル側だけ対象を狭めると「リソース名でしかヒットしない
データセット」がセマンティック検索で見つからない非対称が生じるため。主目的は**検索・発見の精度向上**であり、**AI/MCP エージェント向けの
データセット発見**を一級のターゲットとする。

対象範囲は段階的に広げる:

- **v1: package 単位のベクトルのみ**（1 package = 1 ベクトル、数千〜数万件規模。ベクタ数が
  小さく、厳密検索でも成立する）。リソースメタデータは package のテキストに連結して含め、
  リソース単位の独立ベクトルは作らない
- **後続フェーズ候補: リソース抽出コンテンツ**（kukan-contents 相当）。ただし**テキスト系
  （PDF 等）に限定**する。CSV/TSV 等のテーブルデータは埋め込みによる意味検索が効きにくく、
  こちらは ADR-032 のスキーマ + クエリ経路が担当領域である。

### 前提知識: 埋め込みモデルと生成 AI モデルは独立

埋め込みベクトルは**埋め込みモデルごとに固有**であり、モデル間に互換性はない（次元数も
ベクトル空間の幾何も異なる。共通で使える標準ベクトル形式は存在しない）。一方、検索結果を
消費する**生成 AI（Claude/GPT/ローカル LLM 等）側に埋め込みモデルを合わせる必要はない** —
生成 AI が読むのは検索が返すテキストであり、ベクトルには触れない。したがって:

- クエリと文書は**同一の埋め込みモデル**で埋め込む必要がある（環境内で閉じていればよい）。
- 埋め込みモデルの変更 = **全件再埋め込み + インデックス再構築**。
- MCP を叩いてくる生成 AI が何であるかは埋め込みモデル選定に影響しない。

## 検討した選択肢

### 1. 埋め込みモデルの配備方式

#### 方式 U: 統一セルフホスト

オープンウェイトモデル（bge-m3 等）を両環境でセルフホストする。オンプレは Ollama、
AWS は ECS Fargate 上の Ollama / HuggingFace TEI コンテナ。埋め込みモデルは小さく
（bge-m3 で 568M パラメータ）CPU 推論で実用になるため技術的には成立する。

- 利点: 全環境で検索品質が同一、評価が 1 モデル分で済む、モデル廃止リスクなし。
- 欠点: **AWS に常時起動の推論サービス（Fargate タスク）が 1 つ増える**。

#### 方式 D: 環境別分離（採用）

| 環境          | 埋め込み手段                                                  | 備考                             |
| ------------- | ------------------------------------------------------------- | -------------------------------- |
| AWS           | Bedrock（Titan Embeddings v2 / Cohere embed-multilingual v3） | 従量課金・インフラ追加ゼロ       |
| オンプレ/閉域 | Ollama（bge-m3 第一候補 / multilingual-e5 次点）              | ローカル完結・外部送信なし       |
| AI なし環境   | NoOp → **ベクトル検索無効・BM25 のみに degrade**              | 検索機能自体は全環境で動作を維持 |

- 利点: **新規インフラ（Fargate タスク）を増やさない**。既存 AIAdapter の各実装
  （bedrock / ollama / noop）の延長で実現できる。
- 欠点: 環境間でベクトルの互換性がなく、評価が 2 モデル分になる。
  - ただし**インデックス互換性は実運用で不要**（各デプロイは自前の検索バックエンドに
    インデックスを持ち、環境間でベクトルを持ち運ぶ場面がない）。
  - 評価コストはゴールデンセット評価の**スクリプト化**で 2 モデル目以降を安価にする。

> **決定**: 「新規 Fargate タスクを増やさない」を制約とし、**方式 D（分離）を採用**。
> 方式 U は将来 AWS 側で精度・コスト要件が変わった場合の乗り換え先として温存する。

#### モデル選定の基準（優先順）

1. **ハードフィルタ**: デプロイ環境で動くこと（Bedrock 提供 / Ollama 実行可）、
   日本語 retrieval 性能（JMTEB の Retrieval スコアで比較。平均スコアではなく検索タスクを見る）、
   商用利用可能なライセンス（ローカルモデル。e5 系・bge-m3 は MIT）。
2. **比較基準**: 入力トークン上限（PDF コンテンツを見据えると 8192 対応が有利）、
   次元数（インデックスサイズ・メモリに直結。1024 以下が無難。Matryoshka 対応なら縮小余地）、
   コスト（クエリ側は検索毎に発生）、クエリ埋め込みレイテンシ、
   非対称検索プレフィックス（e5 の `query:`/`passage:` 等）、提供の継続性（deprecation = 全件再埋め込み）。
3. **最終判断はゴールデンセット評価**（後述）で行い、ベンチマークは絞り込みにのみ使う。

### 2. ベクトルの保存先

#### 方式 B: バックエンド別（各検索バックエンドが自前でベクトルを持つ）

| バックエンド | 方式               | 備考                                                                              |
| ------------ | ------------------ | --------------------------------------------------------------------------------- |
| OpenSearch   | k-NN（knn_vector） | HNSW はオフヒープメモリを消費。小規模インスタンスでは注意。                       |
| PostgreSQL   | pgvector           | Aurora Serverless v2 対応済。オンプレは Docker イメージを pgvector 同梱版へ変更。 |

- 利点: 検索と融合が各バックエンド内で完結する（OpenSearch は search pipeline まで使える）。
- 欠点: ベクトル実装が 2 つ必要。OpenSearch k-NN（HNSW）はオフヒープメモリを消費し、
  過去の小インスタンスでのヒープ枯渇障害の教訓と相性が悪い。OpenSearch のインデックス
  再構築時にはベクトルの再投入も必要になる。

#### 方式 P: pgvector 一本化（採用）

ベクトルは全環境で PostgreSQL（pgvector）に置き、OpenSearch は BM25 + ファセット専任の
ままとする。ベクトルは package テーブルの列として Drizzle スキーマで管理し、モデル名・
次元数・コンテンツハッシュを同じ行に持つ。

- 利点:
  - **ベクトル検索の実装が全環境共通**になる。環境差はキーワード側（既存のまま）に限定され、
    実装・テスト量がほぼ半減する。
  - OpenSearch のメモリサイジングを変更しなくてよい（k-NN のオフヒープ消費を回避）。
  - 埋め込み更新がメタデータと**同一 DB でトランザクショナル**（OpenSearch への二重書き込み
    不要）。OpenSearch の rebuild でもベクトルは失われない。
  - drizzle-orm は pgvector の `vector` 型をサポートしており、マイグレーションは既存フローのまま。
- 欠点 / トレードオフ:
  - AWS 構成ではハイブリッド融合がストア横断になり、**RRF をサービス層で実装**する
    （OpenSearch の search pipeline は使わない）。ただし PG フォールバック環境はどのみち
    手動 RRF なので、融合ロジックを全環境共通にできる利点と裏表。
  - 検索 1 回あたり OpenSearch + PG の 2 クエリになる（並列実行し、支配項はクエリ埋め込みの
    レイテンシなので体感影響は軽微）。
  - ベクトル検索側にも可視性フィルタが必要（PG アダプターの `SearchFilters` WHERE 実装を流用）。
  - 検索負荷が本体 DB に乗る。v1 のメタデータ規模では誤差だが、後続フェーズの PDF コンテンツ
    埋め込み（ベクトル数が 2〜3 桁増え HNSW が必要になる）まで PG に載せるかはその時点で再評価する。

> **決定**: PG/Aurora 側にリソース余裕があること、実装の共通化メリットが大きいことから
> **方式 P（pgvector 一本化）を採用**。v1 の対象（package メタデータ）はベクタ数が小さいため
> HNSW インデックスは張らず、フィルタ + 全件距離計算（exact search）で運用する。

### 3. 埋め込み生成のタイミング

- **文書側**: メタデータ CUD 時の BM25 インデックス更新（API ルート・同期）はそのまま維持し、
  **埋め込み生成は QueueAdapter 経由の非同期ジョブ**とする（結果整合）。埋め込みは外部 API
  呼び出しを含みレイテンシ・失敗モードがあるため、リクエストパスに含めない。
  再埋め込みスキップのため**原文のコンテンツハッシュ**を保存する。
- **クエリ側**: 検索リクエスト毎に埋め込みが必要（+100〜300ms）。**lru-cache でクエリ埋め込みを
  キャッシュ**する（ADR-004 のユーティリティをそのまま使用）。

### 4. BM25 との融合（ハイブリッド検索）

- BM25（OpenSearch / PG 全文検索）とベクトル（pgvector）をそれぞれ top-k 取得し、
  **サービス層で RRF（Reciprocal Rank Fusion）により融合**する。方式 P によりストアを
  跨ぐため OpenSearch の search pipeline は使わず、融合ロジックは全環境共通とする。
- **完全一致クエリ（データセット名・組織名そのまま）の精度をベクトルが下げ得る**のが既知の
  失敗パターン。融合重みはゴールデンセットで検証し、劣化を検知できるようにする。
- ベクトル検索は top-k しか返さないため、`total` の意味・ページネーション・ファセット集計
  （OpenSearch/BM25 側の集計に残す）・ハイライト非対応（ベクトルヒットに `<mark>` は
  付かない）の扱いを実装時に整理する。

### 5. 評価: ゴールデンセット

クエリと正解データセットのペア（20〜50 問）を人手で作成し、Recall@10 / nDCG@10 を
スクリプトで計測する。クエリ種別として**類義語・自然文・完全一致を必ず混在**させる
（完全一致は劣化検知用）。用途はモデル比較・融合重みチューニング・回帰テストの 3 つ。
分離配備（方式 D）のため AWS 用・オンプレ用の 2 モデルに対して同一セットで実行する。

## 決定

1. **目的**: メタデータのセマンティック検索と AI/MCP エージェント向けデータセット発見。
   v1 は package 単位のベクトルのみ（title / notes / tags + 所属リソースの name / description
   を連結）。リソース CUD 時も親 package を再埋め込みする。
2. **配備は方式 D（環境別分離）**: AWS = Bedrock、オンプレ = Ollama、NoOp 環境 =
   ベクトル検索無効（BM25 のみへ degrade）。新規 Fargate タスクは増やさない。
3. **ベクトルストアは方式 P（pgvector 一本化）**: 全環境で PostgreSQL に保存し、OpenSearch は
   BM25 + ファセット専任を維持する。ハイブリッド融合はサービス層の RRF で全環境共通とする。
4. **ベクトル検索はオプショナル機能**として設計する。SearchAdapter に capability を持たせ、
   埋め込み不可の環境でも検索機能は BM25 のみで完全動作する。
5. **クエリと文書は同一埋め込みモデル**。ベクトルと併せて**モデル名 + 次元数を記録**し、
   不一致を検出可能にする。モデル変更時は全件再埋め込み（専用ジョブ `embed-all-packages`、
   `POST /admin/reindex-embeddings`。検索インデックスの rebuild とは独立 — 埋め込みは
   OpenSearch を使わない）。
6. **文書側埋め込みは非同期**（QueueAdapter 経由・結果整合）、**クエリ側埋め込みは
   lru-cache でキャッシュ**する。
7. **AIAdapter の `embed()` を拡張**する: バッチ埋め込み、クエリ用/文書用の区別
   （モデル別プレフィックス吸収）、モデル名・次元数の公開。アダプターは既存 4 つの
   範囲内であり ADR-005 に抵触しない。
8. **導入前にゴールデンセット評価を実施**し、モデル選定と融合重みを数値で決定する。
   完全一致クエリの劣化がないことを出荷条件とする。
9. **リソースコンテンツ（PDF 等テキスト系）の埋め込みは後続フェーズ**とし、本 ADR の
   範囲外とする。テーブルデータ（CSV/TSV）は埋め込み対象にしない（ADR-032 の担当領域）。

## 影響

- **packages/adapters/ai**: `embed()` の拡張（バッチ・用途区別・モデルメタデータ公開）。
  bedrock / ollama 実装に埋め込みモデル設定を追加。noop は「埋め込み不可」を明示。
- **packages/adapters/search**: ベクトル検索は PostgreSQL 側（pgvector）にのみ実装し、
  可視性フィルタは既存の `SearchFilters` WHERE 実装を流用。OpenSearch 実装は BM25 +
  ファセット専任のまま変更最小。ハイブリッド融合（RRF）はアダプターの上のサービス層に
  置き、全環境共通とする。capability（ベクトル検索可否）を公開。
- **DB / インフラ**: package テーブルにベクトル列とモデル名・次元数・コンテンツハッシュを
  追加（Drizzle マイグレーション）。オンプレ PostgreSQL イメージを pgvector 同梱版
  （`pgvector/pgvector:pg16`）へ変更（compose.yml）。alpine → Debian ベースへの変更で
  collation 実装が変わるため、既存環境は pg_dump/restore または REINDEX が必要
  （開発環境はボリューム再作成で可）。Aurora は拡張有効化のみ（`CREATE EXTENSION vector`）。
  OpenSearch のサイジング変更は不要。
- **worker / queue**: 埋め込み生成ジョブの追加（メタデータ CUD → キュー投入 → 埋め込み →
  ベクトル列更新）。バルク再埋め込みの専用ジョブ。投入は package ごとに 1 分 1 回に抑える
  （一括投入でリソース件数分のジョブが積まれるため）。
- **compose.yml（Ollama）**: Ollama をオプショナルサービス（profiles）として追加し、
  **開発とオンプレで同一構成**にする（dev/prod パリティ。埋め込みは CPU 推論で足りるため
  GPU 設定は不要）。閉域網では `ollama pull` が使えないため、モデルは**オフライン配送**が
  必要（モデル格納ボリュームの事前配布、またはモデル同梱イメージの配布）。
- **セキュリティ/データ主権**: AWS 環境ではメタデータが Bedrock へ送信される（AWS 内で完結）。
  閉域網では Ollama によりローカル完結し、外部送信は発生しない。
- **運用**: 埋め込みモデルの変更手順（再埋め込み）とゴールデンセットの保守が新規の運用項目。

## 評価結果（2026-07-07）

ゴールデンセット（各環境39クエリ = exact / synonym / natural 各13問 + 正解なしクエリ
3問によるノイズ測定）で計測した Recall@10 / nDCG@10。数値は「キーワードのみ →
ハイブリッド」。ゴールデンセット自体はデプロイ固有データのためコミットしない
（`golden-queries.example.yaml` 参照）。

### オンプレ（ローカル実データ166件、bge-m3 @ 0.45）

| type             | R@10                  | nDCG          |
| ---------------- | --------------------- | ------------- |
| exact            | 100% → 100%（無退行） | 100% → 100%   |
| synonym          | 0% → 96%              | 0% → 79%      |
| natural          | 15% → 90%             | 15% → 66%     |
| **overall nDCG** |                       | **38% → 82%** |

### AWS（demo 全国自治体データ181件、モデル対決）

|                            | **Cohere Embed v4 @ 0.3（推奨）**                | Titan v2 @ 0.15（デフォルト） |
| -------------------------- | ------------------------------------------------ | ----------------------------- |
| synonym R@10               | **74%**                                          | 72%                           |
| natural R@10               | **71%**                                          | 66%                           |
| exact R@10                 | 85%（無退行）                                    | 85%                           |
| overall nDCG               | **75%**                                          | 70%                           |
| 正解なしクエリの擬似ヒット | 0〜1件                                           | 1件                           |
| 導入摩擦                   | Marketplace 購読（管理者 invoke 1回 + 伝播待ち） | なし（自動有効化）            |
| 価格 / 1M トークン         | $0.10                                            | $0.02                         |

- **類似度しきい値はモデルごとに分布が別物**で流用不可（bge-m3 の関連ペア 0.47〜0.62 に
  対し、Titan v2 は 0.05〜0.25、Cohere v4 は 0.15〜0.4 に相当質量が分布）。スイープ測定で
  「上限性能の 97〜99% を維持しつつ正解なしクエリが沈黙する」点を採用: bge-m3 = 0.45、
  Titan v2 = 0.15、Cohere v4 = 0.3。実測値は AI アダプターが内部保持し
  （`EmbeddingInfo.recommendedMinSimilarity`）、env で上書き可能。
- **結論**: オンプレ = bge-m3（確定）。AWS デフォルト = Titan v2（摩擦ゼロ優先）、
  品質優先なら Cohere Embed v4 をオプトイン推奨（特に質問文 +5〜12pt）。
  モデル変更時はゴールデンセットで要再測定。
- Cohere v3・multilingual-e5 は入力トークン制限（512）が連結テキスト（〜8K 相当）に
  不足するため対決前に除外。Cohere v4（128K）でこの制約は解消された。

### 再測定（2026-09-12）

同じゴールデンセット（39 問）で測り直した。**ローカルの埋め込みは bge-m3 から Titan v2
（1024 次元）に替わっている**（`AI_TYPE=bedrock`）が、overall nDCG は同値だった。

| 環境                             |          2026-07-07 |                2026-09-12 |
| -------------------------------- | ------------------: | ------------------------: |
| ローカル（166 → **168** 件）     | 38% → 82%（bge-m3） | **38% → 82%**（Titan v2） |
| demo（181 → **184** 件、Cohere） |                 75% |                   **87%** |

内訳（2026-09-12、キーワードのみ → ハイブリッド）:

| type    | ローカル R@10 | ローカル nDCG |  demo R@10 | demo nDCG |
| ------- | ------------: | ------------: | ---------: | --------: |
| synonym |      0% → 92% |      0% → 75% |   0% → 92% |  0% → 82% |
| natural |     15% → 95% |     15% → 72% |   0% → 81% |  0% → 80% |
| exact   |   100% → 100% |   100% → 100% | 88% → 100% | 88% → 98% |

**ゴールデンセットの正解データセット名は、両環境とも全件が現存している**
（ローカル 56/56・demo 41/41）。exact の劣化も無い。

> ハーネスは標準出力にしか書かない。**数値の正本はこの節である。**
> 実行は `pnpm eval:search --base <URL> --file <YAML>`（`--` は挟まない — `parseArgs` が
> 位置引数として弾く）。

### 再測定（2026-09-16）— 接頭辞の除去と「データ」の停止語化

ローカル（167 パッケージ / 482 リソース、Cohere v4、`vector-similarity-notches` = −2、
ゴールデン 51 問 = synonym 13 / natural 13 / exact 13 / word 12）。**前夜に抄録（ADR-053）が
324 件生成し直された後の母集団**であり、前日までの数値とは比較できない（後述の注意 2）。
比較する 2 つの索引は同じ元から `_reindex` して揃えた（注意 1）。

| 索引の条件                                                | word R@10 | word nDCG（kw → hy） | 他種別                        |
| --------------------------------------------------------- | --------: | -------------------: | ----------------------------- |
| 接頭辞の除去なし（`ja_prefix` を外して `_reindex`）       |       76% |            57% → 63% | 不変                          |
| 接頭辞の除去あり（出荷状態）                              |       76% |            57% → 63% | synonym 1 問 −7（順位の入替） |
| 出荷状態 + 「データ」を検索側の停止語に（キーワードのみ） |       76% |                  57% | 0 問が動く                    |

接頭辞の除去（`kuromoji_part_of_speech` で `接頭詞-名詞接続` を索引側・検索側の両方で落とす）
の効果は、ゴールデン上では**ゼロ**（キーワード脚で 1 問 +1、ハイブリッドで 1 問 −7、いずれも
7〜10 位の入れ替え）。形態素解析は意図どおり（お年寄り → 年寄り、ご案内 → 案内、全世帯 →
世帯、お茶・お客は不変）で、価値はハイライトと両側の一貫性にある。**当初この変更に帰した
「word nDCG 72% → 81%」は誤り** — 79% は同時に変えた目盛り −2 の効果（ADR-036）で、残りは
注意 1 のばらつきである。

「データ」の停止語化は 0 問を動かした。「子育てに関するデータ」で目録 CSV が上位に残るのは
停止語の問題ではなく、内容一致の `score_mode: 'max'` の問題である。

**測定器の注意:**

1. **同じ内容の索引でも word nDCG が 54% と 57% に分かれた。** 増分更新で育った索引と
   `_reindex` 直後の索引では BM25 の統計（マージまで削除済み文書を数える）が違い、僅差の
   順位が入れ替わる。12 問の word では 3 pt 未満は雑音。比較する索引は `_reindex`（または
   `_forcemerge`）後に揃える。
2. **抄録が生成し直されると母集団が変わる。** word 問いの多くは抄録の生活語が唯一の
   語彙的な接点で、再生成で文言が変われば（「高齢者（お年寄り）」→「介護サービス事業所」）
   キーワード脚から消える。前日の word R@10 96% → 76% はこれによるもので、正解パッケージ
   自体は健在。ベクトル脚がこれを拾えていない（パッケージ単位のベクトルが下限に届かない）
   ことは、リソース単位のベクトル（ADR-053 §8）を測る動機になる。

### 埋め込みの単位（2026-09-16、オフライン測定）

**パッケージ 1 本のベクトルか、リソースごとのベクトルか**を、実装前にオフラインで比べた。
ローカル（166 パッケージ / 481 リソース、Cohere embed-v4、ゴールデン 51 問）。DB にある
パッケージベクトルをそのまま基準線に使い、候補側だけを組み直したので、**モデルも母集団も
同一**である。コードは書いていない。

#### ベクトル脚のみ、下限 0.30（パッケージへ `max` で集約して比較）

| 埋め込みの単位と素材              | word R@10 | word nDCG | overall nDCG | 下限を 1 件も超えない問い |
| --------------------------------- | --------: | --------: | -----------: | ------------------------: |
| **パッケージ（現行）**            |       65% |       51% |          86% |                      1 問 |
| パッケージ − notes                |       76% |       56% |          88% |                      0 問 |
| リソース + タイトル/タグ/notes/節 |       81% |       64% |          90% |                      1 問 |
| リソースのみ                      |       89% |       69% |          90% |                      0 問 |
| **リソース + タイトル/タグ**      |       89% |   **70%** |      **92%** |                      0 問 |

**寄与は 2 つに分かれ、順序が付く。単位の変更が +13、`notes` を外すのが +5〜6。**
`synonym` / `natural` / `exact` はどの構成でも動かない（96〜100%）。効くのは短い生活語だけで、
それは ADR-053 決定 8 が予測したとおり — 異質なリソースを平均した重心が、どの主題にも
似ていないためである。

**下限を上げるほど差が開く。** パッケージ単位は 0.35 で 51 問中 7 問、0.40 で 11 問が
「ベクトルが 1 件も立たない」状態になるが、リソース単位では 0 問（0.40 で 4 問）。
**短いクエリが下限に届かないのはモデルの限界ではなく単位の問題だった。**

> `notes` は 1 リソースあたり平均 57 字（全体の 16%）で、長さで薄めているのではない。
> 自治体のデータセット説明は「〜に関するデータです」のような定型が多く、**主題を区別しない
> 語で全ベクトルを互いに近づける**。タイトルとタグが害にならないのは、逆に主題を数語で
> 名指すからである。

#### リソース単位のまま見る

集約は golden の正解がパッケージ名だから行うもので、**集約した瞬間にパッケージが単位に
戻る**。下限も表示も、リソース側の分布で決めるべきである。

51 問 × 481 リソース = 24,531 組の類似度: 中央値 0.198 / p90 0.294 / p99 0.433 / 最大 0.801。

| 下限 | 通る組 | 1 問あたり |
| ---: | ------ | ---------: |
| 0.30 | 8.98%  |    43.2 件 |
| 0.35 | 3.83%  |    18.4 件 |
| 0.40 | 1.64%  |     7.9 件 |

**0.30 は緩すぎる。** 1 問で 43 件が通り、融合の前に雑音が入る。母数が 166 から 481 に
増えるので、パッケージ単位の運用点はそのまま使えない。候補は 0.35〜0.40。

正解パッケージ配下のリソースかどうかで測った精度:

| 種別    |  P@1 | P@5 |  MRR |
| ------- | ---: | --: | ---: |
| synonym | 100% | 49% | 1.00 |
| natural |  92% | 46% | 0.95 |
| exact   | 100% | 49% | 1.00 |
| word    |  58% | 40% | 0.69 |

`word` 以外は**1 位がほぼ必ず正解パッケージのリソース**である（P@5 が下がるのは正解が
1〜3 件しかないため）。**どのリソースが当たったかを見せる価値がある** — ADR-050 の
`matchedResources` にそのまま載る形である。

引けた例と引けない例:

- **単位が効いた**: 「積立金」は「公金管理実績 P1 内訳 基金」というシート単位で当たる
  （0.407）。パッケージ単位ではこれが 19 シートの平均に埋もれていた。「外国人」は上位 4 件
  すべてが正解パッケージの別々のリソース
- **主な雑音源は同一シリーズの別年度**。「車椅子」の 1 位（0.380）は同じ調査の別パッケージの
  調査票で、正解（0.366）と僅差。集約すれば一部は消えるが、リソースを見せるなら対処が要る
- **単位では解けないもの**: 「小さい子ども」は上位 5 件すべて公立学校統計で、未就学児と
  小学生を分けられていない（モデルの粒度）。「お年寄り」は正解の抄録が
  「高齢者（お年寄り）」から「介護サービス事業所」に書き換わった影響で、語彙の問題

#### 融合まで通すと、順位の差はほとんど残らない

**上の数字はベクトル脚単独であり、利用者が見る順位ではない。** そこで脚だけを差し替え、
BM25 は実 API から取り、融合は実コードと同じ式で回した（`FUSION_WINDOW` 50、`RRF_K` 60、
票の重み `clamp((sim − floor) / 0.2)`、下限は運用中の 0.25）。

| 種別         | キーワードのみ | パッケージ単位 | リソース単位 |
| ------------ | -------------: | -------------: | -----------: |
| synonym nDCG |             0% |            79% |          80% |
| natural nDCG |            26% |            81% |          79% |
| exact nDCG   |           100% |           100% |         100% |
| word nDCG    |            57% |            63% |          65% |
| word R@10    |            76% |            85% |          89% |
| **overall**  |            45% |        **81%** |      **81%** |

**ベクトル脚で +19 だった `word` nDCG が、融合後は +2 になる。** overall は動かず、動いた問いは
51 問中 12 で、良化 6・悪化 6 と相殺している。理由は単純で、**BM25 が既に見つけている** —
synonym / natural / exact の Recall は天井であり、RRF はベクトル脚の内部順位の改善をほとんど
順位に反映しない。

残るのは 2 つ。**下限に対する頑健性**（0.35 でパッケージ単位 78% に対しリソース単位 80%、
ベクトルが 1 件も立たない問いは 7 対 0）と、**`word` の Recall**（76% → 89%）である。

新しい制約も 1 つ増える。kNN の窓（50 件）をリソースで取ると、同一パッケージの複数リソースが
窓を食い、**候補に上がるパッケージが平均 19.5 件**に減る（パッケージ単位なら最大 50）。
集約してから 50 件を取る方式では 31.9 件に増えるが、融合後は逆に少し悪化した。
**窓の割り当ては実装時の論点である。**

#### 効いたのは単位でも方式でもなく、レッグ重みだった

融合の側を掃引した（下限 0.25、51 問）。`rrf` は現行、`sim` は順位でなく類似度で票を出す方式、
`simnorm` はクエリ内で正規化した類似度。

| 条件                                     | overall | synonym | natural | exact | word |
| ---------------------------------------- | ------: | ------: | ------: | ----: | ---: |
| **現行**（`rrf`、K=60、λ=1、パッケージ） | **81%** |     79% |     81% |  100% |  63% |
| `simnorm`、K=10、**λ=2**、パッケージ     | **91%** |     98% |     95% |  100% |  70% |
| `simnorm`、K=10、λ=3、リソース           | **92%** |     99% |     95% |  100% |  71% |

**埋め込みを一切変えずに overall 81% → 91%。** 伸びは `synonym`（79 → 98）と
`natural`（81 → 95）— **ベクトルが正しく見つけているのに、RRF がその順位を BM25 に潰していた**。
`exact` はすべての条件で 100% を保ち、決定 8 の出荷条件を満たす。

`RRF_K` = 60 は 50 件のリストに対して大きすぎ、1 位と 50 位の票が 1.8 倍しか違わない。RRF が
ほぼ「両方のリストに載っているか」だけを見る状態になっており、キーワードで引けない問い
（synonym は素で nDCG 0%）でベクトルが正解を上位に押し上げきれない。これは残課題 3 が
「レッグ重み付けは既定のまま」として挙げていた点そのものである。

#### 両単位の平坦域は重なる — だから調整は一度でよい

下限 × λ を両単位で測った（`simnorm`、K=10、overall nDCG）。

| 下限＼λ                 |   1 |      2 |      3 |      4 |      6 |
| ----------------------- | --: | -----: | -----: | -----: | -----: |
| **パッケージ単位** 0.25 |  82 | **91** | **91** | **91** | **91** |
| 0.30                    |  81 |     89 |     89 |     89 |     89 |
| 0.35                    |  78 |     85 |     85 |     85 |     85 |
| **リソース単位** 0.25   |  82 |     90 | **92** | **92** | **92** |
| 0.30                    |  82 |     91 |     91 | **92** | **92** |
| 0.35                    |  82 | **91** | **91** | **91** | **91** |
| 0.40                    |  79 |     87 |     87 |     87 |     87 |

**λ と K は単位を変えても再調整が要らない。** λ ≥ 2〜3 で平坦になり、λ = 3〜4 は
**両単位の平坦域に同時に入る**（パッケージ 91 / リソース 92、それぞれの最良値）。段差は
λ 1 → 2 の一段だけである。

**下限は要再調整。** パッケージ単位は 0.25 が最良で上げると単調に落ちるが、リソース単位は
0.25〜0.35 がほぼ同値で 0.40 で初めて落ちる — 母数が 166 から 481 に増えて分布が変わるため。
これは ADR-036 の目盛りオフセットが扱う種類の変更であり、管理画面から動かせる。

> **峰ではなく平坦域を取る。** 51 問に 30 通り以上を当てているので、最良値は過適合しうる。
> 平坦域の値を選べば過適合を避けられ、同時に単位を変えても据え置ける — **2 つの理由が同じ
> 結論を指す。** パッケージ単位だけを見て λ=2 を選ぶと、リソース単位では 90（λ=3 なら 92）。

#### 順位はこの変更の主目的ではない

ゴールデンセットの正解はパッケージ名しか持たないため、**上の数字はすべてパッケージ順位の
評価である。** リソース単位のベクトルが本当に答えるのは別の問いで、そちらを測る道具が無い。

パッケージ内部を区別できているかは測れる。正解パッケージについて、最良リソースの類似度は
同一パッケージの平均を **0.038〜0.047** 上回る（3 本以上を持つ 37 組）。実例:

| 問い                               | 選ばれたリソース         | 最下位                   |
| ---------------------------------- | ------------------------ | ------------------------ |
| 新しくオープンした美容室を知りたい | **新規**施設一覧 0.553   | **廃止**施設一覧 0.398   |
| 汚水はどうやって処理されている?    | 流入水質・放流水質 0.388 | ダイオキシン類測定 0.348 |

**「新しくオープンした」で新規を選び廃止を最下位に置くのは、重心では原理的に出せない** —
同じパッケージの 5 本が 1 本のベクトルに平均されるからである。

これは残課題 4（ベクトルヒットのハイライト非対応、`matchSource` の拡張）に直接効く。現状、
意味検索だけで当たったパッケージは `matchedOn` が空で、**なぜ出てきたのか説明できない**。
ADR-050 の `matchedResources` は「どのリソースが当たったか」の枠を既に持ち、そこには BM25 の
一致だけが載っている。リソース単位のベクトルはその空白を埋める。

#### 測っていないこと

**この環境は、対象としている状況をほとんど含んでいない。** 正解パッケージのリソース数で
分けると:

| 正解パッケージの最大リソース数 | 問数 | パッケージ単位 | リソース単位 |       差 |
| ------------------------------ | ---: | -------------: | -----------: | -------: |
| 1 本                           |   26 |          94.5% |        94.4% |     ±0.0 |
| 2〜4 本                        |    5 |          88.9% |        85.8% |     −3.0 |
| 5〜9 本                        |   20 |          87.6% |        89.4% | **+1.8** |
| 10 本以上                      |    0 |              — |            — |        — |

**51 問中 26 問は正解が 1 リソースのパッケージで、その場合 2 つの単位は同一である。**
10 本以上の問いは 1 つも無い。カタログ側も 166 中 73 が 1 リソース、10 本以上は 2 件（最大
19）。ADR-053 が挙げた「約 100 表を 1 パッケージに持つデータセット」を、この測定は含んで
いない。**融合後 +1 という数字は下限であり、対象状況での値ではない。**

そのほか未測定: 正解リソースを持つゴールデンセット（上の目的を測る道具）、
pgvector の索引（166 → 481 ベクトル、現在は索引なし）、別カタログでの再現。

> これは ADR-034 の実コードではなく、**同じ式を写したシミュレータ**で測った数字である。
> 採用の前に実コードで再測すること。

## 残課題（未解決事項）

1. ~~**モデルの最終選定**~~ → **解決済み**（上記「評価結果」。オンプレ = bge-m3、
   AWS = Titan v2 デフォルト / Cohere Embed v4 推奨オプトイン）。
2. ~~**ゴールデンセットの作成**~~ → **解決済み**（ローカル39問 + demo 39問。
   デプロイごとに作成・非コミットの運用で確立）。
3. **融合パラメータの確定**: しきい値は解決済み（評価結果参照）。RRF の k 値（=60）と
   レッグ重み付けは既定のまま。**2026-09-16 に測ったところ、ここが最大の伸びしろだった** —
   レッグ重みを上げるだけで overall 81% → 91%、埋め込みもアナライザも変えずに（「埋め込みの
   単位」節）。ただし 51 問への過適合を否定できないため、**採るのは峰ではなく平坦域の値**と
   し、別カタログでの再現を確認してから実コードで測り直すこと。
4. **UI の扱い**: ベクトルヒットのハイライト非対応をどう見せるか、`matchSource` の拡張。
   **リソース単位のベクトル（同節）はこれに直接効く** — 意味的に当たったリソースを名指し
   できるので、ADR-050 の `matchedResources` にそのまま載る。現状は `matchedOn` が空で、
   なぜその結果が出たのかを説明できない。
5. **PDF コンテンツ埋め込み**（後続フェーズ）: チャンク設計・スケール・コスト試算。
   ベクトル数が 2〜3 桁増えるため、pgvector（HNSW 追加）か OpenSearch k-NN への分離かを再評価。
6. **関連データセット推薦**: 同じベクトルを流用した「似ているデータセット」表示
   （オフライン類似度計算のみで実現可。検索統合とは独立に着手可能）。
7. **閉域網へのモデル配送手順**: ボリューム事前配布かモデル同梱イメージかの確定と、
   導入手順書への落とし込み。
8. **管理画面からのハイブリッド検索停止**: 品質劣化・コスト異常時に sysadmin が UI から
   ベクトル検索を一括停止できる運用スイッチ。プロバイダ障害はクエリ埋め込みの
   タイムアウト + BM25 フォールバックで自動 degrade するため対象外（env による
   停止スイッチは検討の上不採用とした）。

## 関連 ADR

- ADR-001: Drizzle ORM（ベクトル列のスキーマ管理）
- ADR-004: lru-cache（クエリ埋め込みキャッシュに使用）
- ADR-005: アダプターは 4 つのみ（本 ADR は既存 AIAdapter / SearchAdapter の拡張に収まり、新アダプターは追加しない）
- ADR-009: 日本語全文検索とフォールバック（BM25 側の前提。degrade 思想を踏襲）
- ADR-013: 検索と DB フィルタリングの分離（ハイブリッド検索でも維持）
- ADR-021: リソースコンテンツ全文検索（後続フェーズの PDF 埋め込みの土台）
- ADR-025: OpenSearch parent-child 統合（pgvector 一本化により OpenSearch 側マッピング変更は不要）
- ADR-032: MCP データクエリ基盤（AI エージェントの発見経路として本 ADR が入口を強化）
